"""Test-only support code: shared between cli/tests/*.py and the
repo-root tests/fixtures/ validation harness (scripts/validate_fixtures.py).

Nothing under here is part of the installed `agent_review` package --
it exists purely to make testing against Microsoft Foundry (live or
recorded) practical without spending real API calls on every run.
"""

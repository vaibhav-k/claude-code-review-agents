# Development dependencies: everything in requirements.txt, plus what's
# needed to run the test suite and lint the codebase.
# Mirrors [project.optional-dependencies].dev in pyproject.toml -- keep
# the two in sync.
#
# Install with:
#   pip install -r requirements-dev.txt
# (or `pip install -e ".[dev]"`, which reads pyproject.toml directly).

-r requirements.txt

pytest==9.1.1
ruff==0.16.6
mypy==2.3.1
# Entra ID auth path is exercised by the test suite's construction-time
# validation tests, so it's an unconditional dev dependency (unlike in
# requirements.txt, where it's optional for a plain API-key setup).
azure-identity==1.25.3
# PyYAML (see requirements.txt) ships no inline type hints -- without its
# stub package mypy reports "Library stubs not installed for 'yaml'" on
# suppressions.py.
types-PyYAML==6.0.12
